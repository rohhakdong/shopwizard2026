<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopwizard.profile.mapper.DeptStoreProdMapper">

    <select id="selectList" parameterType="map" resultType="com.shopwizard.profile.model.DeptStoreProd">
        SELECT * FROM shopion.tPrfDeptStoreProd
        <where>
            <if test="pDeptId != null and pDeptId != ''">AND DeptId = #{pDeptId}</if>
            <if test="pStoreCode != null and pStoreCode != ''">AND StoreCode = #{pStoreCode}</if>
            <if test="pProdCode != null and pProdCode != ''">AND ProdCode = #{pProdCode}</if>
        </where>
        <if test="pPageSize != null and pPageSize != ''">LIMIT #{pPageOffset}, #{pPageSize}</if>
    </select>

    <select id="selectCount" parameterType="map" resultType="int">
        SELECT COUNT(*) FROM shopion.tPrfDeptStoreProd
        <where>
            <if test="pDeptId != null and pDeptId != ''">AND DeptId = #{pDeptId}</if>
            <if test="pStoreCode != null and pStoreCode != ''">AND StoreCode = #{pStoreCode}</if>
            <if test="pProdCode != null and pProdCode != ''">AND ProdCode = #{pProdCode}</if>
        </where>
    </select>

    <select id="select" parameterType="map" resultType="com.shopwizard.profile.model.DeptStoreProd">
        SELECT * FROM shopion.tPrfDeptStoreProd WHERE DeptId = #{deptId} AND StoreCode = #{storeCode} AND ProdCode = #{prodCode}
    </select>

    <insert id="insert" parameterType="com.shopwizard.profile.model.DeptStoreProd">
        INSERT INTO shopion.tPrfDeptStoreProd (DeptId, StoreCode, ProdCode, State, Remark, RegistId, RegistName, RegistDate, ChangeId, ChangeName, ChangeDate)
        VALUES (#{deptId}, #{storeCode}, #{prodCode}, #{state}, #{remark}, #{registId}, #{registName}, NOW(), #{changeId}, #{changeName}, NOW())
    </insert>

    <update id="update" parameterType="com.shopwizard.profile.model.DeptStoreProd">
        UPDATE shopion.tPrfDeptStoreProd SET State = #{state}, Remark = #{remark}, ChangeId = #{changeId}, ChangeName = #{changeName}, ChangeDate = NOW()
        WHERE DeptId = #{deptId} AND StoreCode = #{storeCode} AND ProdCode = #{prodCode}
    </update>

    <delete id="delete" parameterType="map">
        DELETE FROM shopion.tPrfDeptStoreProd WHERE DeptId = #{deptId} AND StoreCode = #{storeCode} AND ProdCode = #{prodCode}
    </delete>

</mapper>
