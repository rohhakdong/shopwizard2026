<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopwizard.product.mapper.ProdIngrdOriginMapper">

    <select id="selectList" parameterType="map" resultType="com.shopwizard.product.model.ProdIngrdOrigin">
        SELECT *
        FROM shopion.tPrdProdIngrdOrigin
        <where>
            <if test="pProdCode != null and pProdCode != ''">AND ProdCode = #{pProdCode}</if>
        </where>
        <if test="sidx != null and sidx != ''">ORDER BY IngrdName ASC</if>
    </select>

</mapper>
